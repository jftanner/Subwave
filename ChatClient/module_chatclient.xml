<?xml version="1.0" encoding="UTF-8"?>
<project name="module_chatclient" default="compile.module.chatclient">
    <dirname property="module.chatclient.basedir" file="${ant.file.module_chatclient}"/>


    <property name="compiler.args.chatclient" value="-encoding UTF-8 -source 1.6 ${compiler.args}"/>

    <property name="chatclient.output.dir" value="${module.chatclient.basedir}/../out/production/ChatClient"/>
    <property name="chatclient.testoutput.dir" value="${module.chatclient.basedir}/../out/test/ChatClient"/>

    <property name="chatclient.deployment.dir" value="${module.chatclient.basedir}/../deployment/"/>
    <property name="chatclient.deployment.file" value="${chatclient.deployment.dir}/SubwaveClient.jar"/>
    <property name="chatclient.main-class" value="com.tanndev.subwave.client.ui.tui.ClientTUI"/>

    <path id="chatclient.module.bootclasspath">
        <!-- Paths to be included in compilation bootclasspath -->
    </path>

    <path id="chatclient.module.production.classpath">
        <pathelement location="${common.output.dir}"/>
    </path>

    <path id="chatclient.runtime.production.module.classpath">
        <path refid="common.runtime.production.module.classpath"/>
        <pathelement location="${chatclient.output.dir}"/>
    </path>

    <path id="chatclient.module.classpath">
        <pathelement location="${common.testoutput.dir}"/>
        <pathelement location="${common.output.dir}"/>
        <pathelement location="${chatclient.output.dir}"/>
    </path>

    <path id="chatclient.runtime.module.classpath">
        <path refid="common.runtime.module.classpath"/>
        <pathelement location="${chatclient.testoutput.dir}"/>
        <pathelement location="${chatclient.output.dir}"/>
    </path>


    <patternset id="excluded.from.module.chatclient">
        <patternset refid="ignored.files"/>
    </patternset>

    <patternset id="excluded.from.compilation.chatclient">
        <patternset refid="excluded.from.module.chatclient"/>
    </patternset>

    <path id="chatclient.module.sourcepath">
        <dirset dir="${module.chatclient.basedir}">
            <include name="src"/>
        </dirset>
    </path>


    <target name="compile.module.chatclient"
            depends="compile.module.chatclient.production,compile.module.chatclient.tests"
            description="Compile module ChatClient"/>

    <target name="compile.module.chatclient.production" depends="compile.module.common"
            description="Compile module ChatClient; production classes">
        <mkdir dir="${chatclient.output.dir}"/>
        <javac destdir="${chatclient.output.dir}" debug="${compiler.debug}" nowarn="${compiler.generate.no.warnings}"
               memorymaximumsize="${compiler.max.memory}" fork="true">
            <compilerarg line="${compiler.args.chatclient}"/>
            <bootclasspath refid="chatclient.module.bootclasspath"/>
            <classpath refid="chatclient.module.production.classpath"/>
            <src refid="chatclient.module.sourcepath"/>
            <patternset refid="excluded.from.compilation.chatclient"/>
        </javac>

        <copy todir="${chatclient.output.dir}/com/tanndev/subwave/client">
            <fileset dir="${module.chatclient.basedir}/src">
                <patternset refid="compiler.resources"/>
                <type type="file"/>
            </fileset>
        </copy>
    </target>

    <target name="compile.module.chatclient.tests" depends="compile.module.chatclient.production"
            description="compile module ChatClient; test classes" unless="skip.tests"/>

    <target name="clean.module.chatclient" description="cleanup module">
        <delete dir="${chatclient.output.dir}"/>
        <delete dir="${chatclient.testoutput.dir}"/>
    </target>

    <target name="deploy.module.chatclient" depends="compile.module.chatclient">
        <mkdir dir="${chatclient.deployment.dir}"/>
        <mkdir dir="${chatclient.output.dir}/lib"/>
        <copy todir="${chatclient.output.dir}/lib" flatten="true">
            <path refid="chatclient.module.production.classpath"/>
        </copy>

        <manifestclasspath property="chatclient.manifest.classpath" jarfile="${chatclient.deployment.file}">
            <classpath refid="chatclient.module.production.classpath"/>
        </manifestclasspath>

        <jar destfile="${chatclient.deployment.file}" basedir="${chatclient.output.dir}">
            <manifest>
                <attribute name="Main-Class" value="${chatclient.main-class}"/>
                <attribute name="Class-Path" value="${chatclient.manifest.classpath}"/>
            </manifest>
        </jar>
    </target>
</project>